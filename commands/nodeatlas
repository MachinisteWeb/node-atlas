#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../lib/node_modules/node-atlas/node-atlas.js" "$@"
  ret=$?
else 
  node  "$basedir/../lib/node_modules/node-atlas/node-atlas.js" "$@"
  ret=$?
fi
exit $ret
