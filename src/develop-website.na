/* jshint node: true */
var nodeAtlas = require("../../NodeAtlas/"),
	versionFrench = new nodeAtlas(),
	versionEnglish = new nodeAtlas();

versionFrench.started(function () {
	versionEnglish.run({
		"webconfig": "webconfig.en-us.json"
	});	
}).run({
	"browse": true
});